version: '3.8'

services:
  # Base de datos MySQL
  clients-db:
    image: mysql:8.0
    container_name: clients-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: clients
      MYSQL_CHARACTER_SET_SERVER: utf8mb4
      MYSQL_COLLATION_SERVER: utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    volumes:
      - clients-db-data:/var/lib/mysql
      - ./src/main/resources/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - clients-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # API Spring Boot
  clients-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clients-api
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:mysql://clients-db:3306/clients?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
    ports:
      - "8081:8081"
    depends_on:
      clients-db:
        condition: service_healthy
    networks:
      - clients-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  clients-db-data:
    driver: local

networks:
  clients-network:
    driver: bridge
